# 開發用的 C++ 工具（Python 工具在 tools/eval、tools/fetch_models，不需要建置）
add_subdirectory(capture_bench)
add_subdirectory(ocr_cli)
add_subdirectory(perf_report)
add_subdirectory(shape_bench)
add_subdirectory(translate_cli)
