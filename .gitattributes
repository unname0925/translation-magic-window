* text=auto eol=lf

# Windows 指令檔必須是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二進位檔
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.onnx binary
*.zip binary

# 第三方原始碼：原封不動保存（不轉換換行），才能用上游的 SHA-256 驗證
third_party/** -text
